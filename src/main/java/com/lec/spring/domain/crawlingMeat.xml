<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.lec.spring.crawling.Jin_CrawlingDAO">

	<insert id="insertMeat" flushCache="true" 
		parameterType="com.lec.spring.crawling.Jin_CrawlingDTO"
		keyProperty="pro_uid" useGeneratedKeys="true" keyColumn="pro_uid">
		INSERT INTO ic_product
		(pro_name, pro_price, pro_img, pro_content,pro_kind)
		VALUES
		(#{pro_name}, #{pro_price}, #{pro_img}, #{pro_content},"정육")
	</insert>
	
	<select id="selectMeat" resultType="com.lec.spring.domain.JinMeatDTO">
		SELECT 
			pro_uid "uid", 
			pro_name name, 
			pro_img img, 
			pro_price price 
		FROM 
			ic_product 
		WHERE
			pro_kind = "정육"
		ORDER BY 
			pro_uid ASC	
	</select>

</mapper>

	